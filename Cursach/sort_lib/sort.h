#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>

extern void swap(uint32_t *arr, int i, int j);
extern void bubbleSort(uint32_t *arr, int n);
extern void Quick_sort(uint32_t *arr, int low, int high);